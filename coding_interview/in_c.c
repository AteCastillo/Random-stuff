#include <stdio.h>
/* this program is just to print a few things...*/
int main(void)
{
    int num = 4, num1 = 6, sum, f;
    float c;

    c = (f - 32) / 1.8;
    
    sum = num + num1;
    printf("you are awesome %i sum is: %i \n ", num, sum);
    printf("faren = %f \n", c);
    return 0;
}

